# 代码审查报告 — 竞品分析模块评审意见采纳（结构统计 + 指纹增量 + 校准工具）

| 项 | 内容 |
|---|---|
| 审查对象 | `optimize-competitor-analysis` 分支本轮增量（未提交工作区 → 本次提交） |
| 审查轮次 | 自审 + 独立交叉审查（reviewer 代理，2026-08-24） |
| 测试结果 | 竞品模块 56/56 通过；后端全量（除环境受限的 test_auth_scoping / main 既有失败）无新增失败；前端 vite build 通过 |
| 审查工具 | WorkBuddy（AI 代码审查） |

## 一、改动范围（外部评审 Kimi/claw 意见采纳项）

1. **指纹库增量**：DITA-OT（PDF producer 三种形态 + oXygen creator）、Prince XML；HTML 侧 DITA 证据链（URL /topics/、/concepts/、/tasks/、topic/concept/task 类名、data-dita-* 属性、meta generator），结构特征与 generator 同时命中时证据合并、不产生重复条目
2. **结构统计（客观指标）**：`analyze_structure()` 输出页数/章节数/图片数/表格数/安全警告数；报告新增「二、结构统计」章节，可读性/启示重编号为三/四；前端详情页同步展示
3. **CLI 校准脚本**：`scripts/calibrate_competitor.py`，批量输出原始指标 + 评分 + 中位数分布供人工复核阈值
4. **算法文档**：`docs/竞品文档分析算法设计说明_V1.0.md`，公开全部公式/权重/阈值/置信度规则/已知局限

## 二、问题与修复（单表，按用户偏好不重复展开）

| 级别 | 位置 | 问题 → 修复 |
|---|---|---|
| P0 | competitor_analysis.py 中文警告正则 | 「注意事项」「注意观察仪器状态」等普通词组全部误计为警告 → 标签后必须为标点/空白/行尾（lookahead），补回归测试 |
| P1 | competitor_analysis.py `_count_pdf_tables` | 旧版 PyMuPDF 无 find_tables 时 AttributeError 被内层 except 吞掉，静默返回 0 当真实计数 → 循环前 hasattr 检测，返回 None + 说明 |
| P1 | competitor_html.py DITA 类名正则 | `\b` 词边界使 `class="task-list"` 等 GFM 扩展类名误报 → 负向前瞻 `(?![-\w])` 排除 |
| P1 | competitor_analysis.py 同步性能 | ≤300 页全量 find_tables 可给同步 POST 加数十秒 → 增加 10 s 时间预算，超限提前结束并注明计数可能偏低 |
| P1 | 测试缺口 | 无 HTML 流程 structure_stats 注入测试、无 find_tables 不可用路径测试 → 补 `_run_analysis` 全链路断言（含报告三章节编号）与 FakeDoc 降级测试 |
| P2 | Competitor.vue ×2 处 | 前端写死「二、可读性分析」与报告编号不一致 → 新增结构统计展示块（旧任务无该字段自动隐藏），编号动态适配 |
| P2 | competitor_insight.py docstring | 仍写「三、对本司的启示」→ 更正为四 |
| P2 | 算法文档 | 模式数与实际不符（22→25 条/19 工具）、`/topics//concepts/` 连写笔误 → 更正 |
| P2 | 标题计数 | 目录页 TOC 与正文标题重复计数 → 按标题文本去重 |

## 三、审查通过项（单行列举）

边界降级（空文本/损坏 PDF/DOCX/MD 路径）、存量数据兼容（旧任务无 structure_stats 正常、schema 无字段限制、analyze_document 新参可选向后兼容）、安全（notes/证据串均经 _md_escape，无新增注入/SSRF 面）、正则无灾难性回退、CLI 单文件失败不中断整批、prince/oxygen 子串在 producer/creator 语境误匹配风险可接受。

## 四、遗留事项

- 真实手册集校准未执行（待用户提供竞品 PDF 样本后运行 `scripts/calibrate_competitor.py`）
- `_run_analysis` 中 PDF 被打开两次（工具识别 + 结构统计），可在后续轮次合并句柄（本轮以时间预算兜底）
- 章节数为启发式近似值，报告中已注明口径

审核工具：WorkBuddy（AI 代码审查）
