# NextSeq 1000/2000 HTML 竞品分析报告

## 1. 输入说明

- 分析对象：`https://support-docs.illumina.com/IN/NextSeq10002000/Content/IN/FrontPages/NextSeq10002000.htm`
- 页面标题：`NextSeq 1000/2000`
- 分析时间：`2026-08-25`
- 分析形态：单个 HTML 首页

## 2. 关键结论

- 这次成功分析的是帮助集首页，不是整套帮助站的所有 topic 页面。
- 首页文本长度仅约 `842` 字符，主体内容以导航入口和章节名为主，缺少步骤正文。
- 因为输入是首页，`Findability` 和 `Usability` 的分数偏保守，更适合解释为“首页可见信息质量”，不适合直接代表整站完整体验。

## 3. 工具与来源判断

- 来源类型：`HTML`
- Producer：`Web page`
- Creator：`support-docs.illumina.com`
- 工具识别结论：主编辑工具：MadCap Flare（high 置信）

说明：

- 当前规则引擎已能根据 URL 目录、`.htm` topic 结构和 MadCap 运行时标记识别 Flare。
- 该页面属于帮助集首页，内容层仍偏入口页。

识别证据：

- URL 路径含 Flare 导出目录特征 `/Content/`
- Topic 页以 `.htm` 结尾且位于 `/Content/` 目录
- HTML 引用 MadCap 运行时脚本或标记

## 4. 结构统计（客观指标）

| 指标 | 数值 | 口径说明 |
| --- | --- | --- |
| 页数 | `1` | 文档总页数 |
| 章节数 | `0` | 标题行启发式识别，近似值 |
| 图片数 | `0` | HTML `<img>` 统计 |
| 表格数 | `0` | HTML `<table>` 统计 |
| 安全警告数 | `0` | 行首 WARNING/CAUTION/DANGER/NOTICE/警告/注意/危险 |

> 安全警告数为 0：检测依赖文本关键词匹配，图标或特殊排版呈现的安全提示可能未被统计，建议人工复核。

> 文本样本量有限（7 句），评分仅供参考；建议补充更多正文内容后复核。

> 当前页面路径疑似手册入口页/封面页，非正文内容；建议选择具体子页面分析，或上传完整 HTML 包后分析。

## 5. 可读性分析

- 综合评分：`89.3`
- 评级：`excellent`
- 语言：英文

基础统计：

- 句子数：`7`
- 平均句长：`14.9` 词
- 平均段落长度：`3.9` 词
- 术语密度：`48.5%`
- 被动句比例：`0.0%`

解读：

- 句长、段落长度和被动句比例都很好，所以总分较高。
- 术语密度异常高，原因是首页几乎全是产品名、章节名和导航项。
- 这个分数反映的是“首页文案短而清晰”，不是“整套文档正文都更易读”。

## 6. Access / Findability / Usability

### 5.1 Access

- 分数：`63.3`
- 评级：`fair`

观察：

- HTML 页面可直接在线访问，输入层面可获得性成立。
- 当前首页文本中没有明显版本号或修订号，所以版本透明度得分偏低。
- 规则引擎当前仍按单页形态计分，尚未覆盖整站入口、语言切换和访问门槛等站点级能力。

### 5.2 Findability

- 分数：`0.0`
- 评级：`poor`

观察：

- 规则引擎这次只抽取到了首页正文文本，没有抓到完整站内目录树、面包屑或搜索框信号。
- 从页面可见文本看，首页列出了主要章节入口，例如 `System Overview`、`Safety & Compliance`、`Troubleshooting`。
- 当前 `0.0` 更像“单页抽取结果中未识别到显式目录结构”，不等于整站没有查找能力。

### 5.3 Usability

- 分数：`54.2`
- 评级：`poor`

观察：

- 首页主要承担入口导航作用，本身几乎没有操作步骤，所以步骤完整性和可操作指令得分低。
- 页面已经显式暴露 `Troubleshooting`、`Maintenance`、`Sequencing Output` 等任务入口，这是正向信号。
- 如果继续抓取二级 topic 页面，`Usability` 分数大概率会明显高于首页结果。

## 7. 与 PDF 版本对比

- PDF 版本更适合做“整本文档能力”分析，因为正文覆盖完整。
- HTML 首页更适合观察“站点入口设计”和“章节导航暴露方式”。
- PDF 中可见的结构优势在 HTML 首页依然存在：章节路径清晰，任务分区明确。
- HTML 首页当前分数偏低的主因是输入粒度过小，不是文档体系本身弱。

## 8. 核心 Topic 抽样

为避免首页样本过薄，我追加抽了 3 个核心 topic 页面：

| 页面 | 文本长度 | 可读性 | Access | Findability | Usability |
| --- | --- | --- | --- | --- | --- |
| `System Overview` | `2692` | `86.2` | `83.3` | `50.0` | `54.2` |
| `Sequencing Protocol` | `1243` | `92.7` | `83.3` | `0.0` | `44.2` |
| `Troubleshooting` | `675` | `90.2` | `83.3` | `0.0` | `54.2` |

补充观察：

- `System Overview` 已经能稳定识别到说明性正文，所以 `Findability` 回升到 `50.0`。
- `Sequencing Protocol` 和 `Troubleshooting` 页面标题与主题清晰，但当前单页抓取文本仍然缺少完整站内导航和大量步骤块，导致 `Findability` 与 `Usability` 继续保守。
- 三个 topic 页都验证了同一个方向：HTML 帮助站的真实内容能力强于首页分数，当前偏低项主要来自“单页文本抽取”而不是站点本身。

## 9. 结论

这次 HTML 重跑已经成功，且验证了一个关键事实：`NextSeq 1000/2000` 的帮助体系原始来源确实是可访问的 `support-docs.illumina.com` topic 页面。当前这个首页样本更适合判断“导航骨架”，不适合单独代表整站 Findability 和 Usability。要得到更接近真实站点体验的结果，下一步应该继续抓取 3 到 5 个核心 topic 页面联合评分。
