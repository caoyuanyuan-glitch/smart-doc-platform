# WorkBuddy Review

- Branch: 260803-feat-smart-polish
- Commit: a725773

## Context

本轮提交聚焦 CAT 候选质量与结果落地链路，重点包括：

1. 候选过滤前后增加结构化规则，拦截片段候选、原文等价候选、关键实体错配和技术术语覆盖不足的候选。
2. CAT analyze 返回候选过滤摘要、术语命中数量和 `needs_review` 标记，前端同步展示“需确认”和过滤统计。
3. CAT apply 在候选只覆盖前半句时自动补回原文尾部，减少半句丢失。
4. DOCX 读写统一只遍历正文段落，避免表格文本污染 analyze/apply 索引。
5. CAT 结果生成后持久化 `PolishedDocument`，前端可直接进入预览页。
6. AIClient 支持调用方传入超时，CAT 评分可单独放宽 provider 超时。

## Review Focus

1. `backend/app/api/polish.py`
- 关注过滤阈值是否过严，是否进一步压低了真实样本命中率。

2. `backend/app/api/polish.py`
- 关注 `_merge_replacement_with_source_tail(...)` 是否可能在个别句型上拼接出重复尾句。

3. `frontend/src/views/Polish.vue`
- 关注 `candidateDebugSummary` 和 `needs_review` 展示是否足够清晰，是否还需要更直观的诊断入口。

4. `backend/app/utils/ai_client.py`
- 关注 provider 级 timeout 放宽后是否会带来其他调用链的等待时间回归。
