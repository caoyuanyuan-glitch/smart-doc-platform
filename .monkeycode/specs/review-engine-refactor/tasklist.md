# 需求实施计划

- [ ] 1. 设置审核引擎基础结构
  - [ ] 1.1 创建 `backend/app/review_engine/` 包和核心数据模型
    - 定义 `CandidateIssue`、`ValidationResult`、`ReviewStageDiagnostics` 等轻量结构
    - 覆盖 Requirements 1.2、3.1、7.1
  - [ ] 1.2 创建验证层模块 `validation.py`
    - 提供原文定位、建议有效性、敏感实体保护和低价值 AI 输出过滤入口
    - 覆盖 Requirements 3.2、3.3、3.4、3.5
  - [ ]* 1.3 为验证层补充单元测试
    - 覆盖数字改写、术语改写、建议等于原文、缺失原文等样例

- [ ] 2. 接入现有审核流程
  - [ ] 2.1 在 `backend/app/api/review.py` 中调用验证层
    - 保持现有 API 和数据库模型兼容
    - 覆盖 Requirements 1.1、3.2、3.3、3.4
  - [ ] 2.2 保存阶段级诊断摘要
    - 在 `Review.summary` 中追加阶段数量、过滤数量和耗时的 JSON 字段
    - 覆盖 Requirements 1.2、7.1、7.2
  - [ ]* 2.3 编写流程级集成测试
    - 使用固定文档内容验证规则结果、AI 候选和过滤结果

- [ ] 3. 建立最小评测能力
  - [ ] 3.1 新增 `backend/scripts/evaluate_review.py`
    - 支持读取已有 `review_id` 并输出问题数、无效建议数、数字改写数和敏感实体改写数
    - 覆盖 Requirements 5.1、5.2、5.3
  - [ ] 3.2 支持当前 CSS 文档的快速回归检查
    - 检查截图点名误报、无效建议和数字改写残留
    - 覆盖 Requirements 5.4、7.4
  - [ ]* 3.3 扩展评测集配置文件
    - 支持多个文档、标准答案、允许忽略项和明确误报项

- [ ] 4. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [ ] 5. 后续迁移准备
  - [ ] 5.1 标记待迁移的确定性规则分组
    - 单位、日期、URL、型号、版权年份、修订历史、图表编号分组迁移
    - 覆盖 Requirements 2.1、2.2、2.4
  - [ ] 5.2 保留兼容层和回滚路径
    - 当前路由继续调用原入口，新引擎模块以函数级接入方式逐步替换
    - 覆盖 Requirements 1.3、6.2
